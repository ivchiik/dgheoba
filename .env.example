# Copy to .env and fill in. Everything with the EXPO_PUBLIC_ prefix is inlined into the
# JS bundle at build time and is readable by anyone with the app — never put secrets here.
EXPO_PUBLIC_API_URL=https://api.example.com
