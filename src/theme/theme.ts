import { COLORS } from "./colors";
import type { Theme } from "./theme.types";

export const FONT_FAMILY = "FiraGo-Regular";

export const theme: Theme = {
  color: COLORS,
  fontFamily: FONT_FAMILY,
};
